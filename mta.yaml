ID: GitMerge
_schema-version: '2.0'
version: 0.0.1

modules:
 - name: fioriapp
   type: html5
   path: fioriapp
   parameters:
      disk-quota: 256M
      memory: 256M
   build-parameters:
      builder: grunt
